# Experiments

Ad-hoc, manually-run scripts used while developing search, guard prompts and
encryption. They are **not** part of the automated test suite (`tests/`) and
may depend on a running Qdrant/Postgres or on the `/app` Docker layout.
